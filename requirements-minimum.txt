# LOCK FILE - generated by Phase 9 issue #27
# NOTE: this is a MINIMUM-VERSIONS file, not a lock file (audit B0.7).
# For reproducible measurements use the per-run environment lock
# (benchmarks/results/industrial/runs/<identity>/distribution-freeze.txt).
# OpenLithoHub (dynamic version)

numpy>=1.24
torch>=2.12
typer[all]>=0.9
pydantic>=2.0
rich>=13.0
diff-surrogate @ git+https://github.com/telleroutlook/diff-surrogate.git@6b0ec10916f58297a0b48cb3af470e7d1ad99b45
